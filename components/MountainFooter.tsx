// Spacer that reveals the fixed ski map behind the page content on scroll
// Spacer that reveals the fixed ski map behind the page content on scroll
export default function MountainFooter() {
  return (
    <div style={{ height: "clamp(200px, 42vw, 428px)", flexShrink: 0 }} />
  );
}
